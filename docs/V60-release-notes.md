# V60 — Reference-driven Public View preparation

The user supplied the first 2 of 7 screenshots that define the desired final Public View from top to bottom. V60 intentionally does **not** lock the final visual section order yet.

Changes:
- Stored the first two supplied reference images under `docs/ui-reference/`.
- Added a written reference plan capturing the visible hierarchy in images 01–02.
- Added `frontend/src/publicNavConfig.js` as a data-driven menu/submenu foundation.
- Kept the existing Public View unchanged until all 7 reference images are received.
- Updated API health/version marker to V60.
- Added `scripts/qa_v60.mjs` for reference and navigation preparation checks.
